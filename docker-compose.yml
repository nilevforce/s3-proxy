version: "3.9"

services:
  s3-proxy:
    container_name: s3-proxy
    restart: unless-stopped
    build: .
    # Удалите ports или используйте expose,
    # чтобы сделать сервис доступным только внутри докера
    ports:
      - "8000:8000"
